import type { ReactNode } from "react";
import { Button } from "@/components/ui/button";
export function AdminField({ label, name, defaultValue, type = "text", required = false, children }: { label: string; name: string; defaultValue?: string | number | readonly string[]; type?: string; required?: boolean; children?: ReactNode }) { return <label className="grid gap-2 text-sm font-semibold text-foreground"><span>{label}</span>{children ?? <input className="min-h-11 rounded-lg border border-input bg-card px-3 text-base font-normal outline-none focus-visible:border-ring focus-visible:ring-2 focus-visible:ring-ring/25" defaultValue={defaultValue} name={name} required={required} type={type} />}</label>; }
export function CheckField({ label, name, defaultChecked }: { label: string; name: string; defaultChecked?: boolean }) { return <label className="flex min-h-11 items-center gap-3 text-sm font-semibold"><input className="size-4 accent-primary" defaultChecked={defaultChecked} name={name} type="checkbox" />{label}</label>; }
export function Submit({ label = "Save changes" }: { label?: string }) { return <Button type="submit">{label}</Button>; }
export function MoneyField({ label, name, paise }: { label: string; name: string; paise?: number }) { return <AdminField label={`${label} (INR)`} name={name} defaultValue={paise === undefined ? "" : (paise / 100).toFixed(2)} type="number" required />; }
