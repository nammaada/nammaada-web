import { PageHeader } from "@/components/admin/page-header";
import { Card } from "@/components/ui/card";
import { adminRows } from "@/lib/admin/data";
type Row={id:string;full_name:string;phone:string;email:string|null;district_city:string;state:string;pincode:string};
export default async function CustomersPage() { const rows=await adminRows<Row>("admin_customers"); return <><PageHeader title="Customers" description="Guest customer records captured through real orders."/><Card className="overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[44rem] text-left text-sm"><thead className="border-b border-border bg-secondary/50 text-xs uppercase tracking-wider text-muted-foreground"><tr><th className="px-5 py-4">Customer</th><th className="px-5 py-4">Phone</th><th className="px-5 py-4">Email</th><th className="px-5 py-4">Location</th></tr></thead><tbody className="divide-y divide-border">{rows.map(r=><tr key={r.id}><td className="px-5 py-4 font-semibold">{r.full_name}</td><td className="px-5 py-4">{r.phone}</td><td className="px-5 py-4">{r.email??"—"}</td><td className="px-5 py-4">{r.district_city}, {r.state} {r.pincode}</td></tr>)}</tbody></table>{rows.length===0?<p className="p-12 text-center text-sm text-muted-foreground">No customers yet.</p>:null}</div></Card></>; }
