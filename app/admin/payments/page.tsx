import { PageHeader } from "@/components/admin/page-header";
import { Card } from "@/components/ui/card";
import { adminRows, formatINR } from "@/lib/admin/data";
type Row={id:string;order_id:string;amount_paise:number;currency:string;status:string;razorpay_order_id:string|null;razorpay_payment_id:string|null;signature_verified:boolean;created_at:string};
export default async function PaymentsPage() { const rows=await adminRows<Row>("admin_payments"); return <><PageHeader title="Payments" description="Read-only payment records. Razorpay integration and verification are deferred."/><Card className="overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[54rem] text-left text-sm"><thead className="border-b border-border bg-secondary/50 text-xs uppercase tracking-wider text-muted-foreground"><tr><th className="px-5 py-4">Amount</th><th className="px-5 py-4">Status</th><th className="px-5 py-4">Razorpay order</th><th className="px-5 py-4">Verified</th><th className="px-5 py-4">Created</th></tr></thead><tbody className="divide-y divide-border">{rows.map(r=><tr key={r.id}><td className="px-5 py-4 font-semibold">{formatINR(r.amount_paise)} {r.currency}</td><td className="px-5 py-4">{r.status}</td><td className="px-5 py-4 text-xs">{r.razorpay_order_id??"—"}</td><td className="px-5 py-4">{r.signature_verified?"Yes":"No"}</td><td className="px-5 py-4">{new Date(r.created_at).toLocaleDateString("en-IN")}</td></tr>)}</tbody></table>{rows.length===0?<p className="p-12 text-center text-sm text-muted-foreground">No payments yet.</p>:null}</div></Card></>; }
